# Deployed backend's base URL (Railway/Render). Baked into the client
# bundle at build time — see ARCHITECTURE.md "Footguns actually hit" for why this
# must be set before/during the build, not just as a runtime container var.
NEXT_PUBLIC_API_URL=
